[package]
name = "deploy2monster"
version = "1.0.0"
edition = "2024"

[dependencies]
serde      = { version = "1", features = ["derive"] }
serde_json = "1"
suppaftp   = "6"     # FTP/FTPS - mais mantido que o crate ftp
mysql      = "25"    # driver MySQL nativo